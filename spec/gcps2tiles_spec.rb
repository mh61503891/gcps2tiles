require 'spec_helper'

describe Gcps2tiles do
  it 'has a version number' do
    expect(Gcps2tiles::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
